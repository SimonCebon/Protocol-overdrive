services:
  game:
    build: .
    volumes:
      - .:/app
      - /tmp/.X11-unix:/tmp/.X11-unix
    environment:
      - DISPLAY=host.docker.internal:0.0
      - SDL_VIDEODRIVER=x11
    working_dir: /app
